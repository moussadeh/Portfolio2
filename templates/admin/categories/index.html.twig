{% extends "@EasyAdmin/page/content.html.twig" %}
{% block content_title %}Catégories{% endblock %}

{% set actionName = 'categories' %}
{% block page_actions %}
    {# <a class="btn-primary" href="{{path('admin_projects_new')}}">Add</a> #}
{% endblock %}

{% block main %}
    <table class="datagrid">
        <thead>
            <tr>
                <td>Code</td>
                <td>Name</td>
                <td>Action</td>
            </tr>
        </thead>
        <tbody>
            {% for data in categories %}
                <tr>
                    <td>{{ data.code ?? "" }}</td>
                    <td>{{ data.name ?? "" }}</td>
                    <td>
                        <a class="btn btn-secondary" href="#"><i class="fa-solid fa-pen"></i></a>
                        <a class="btn btn-danger" href="#"><i class="fa-solid fa-trash"></i></a>
                    </td>
                </tr>   
            {% endfor %}
        </tbody>
    </table>
{% endblock %}